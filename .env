# Local default: `docker compose up` starts the seeded demo source database too.
# Production deployments should clear this (e.g. `COMPOSE_PROFILES= docker compose up -d`)
# so the demo container never starts there.
COMPOSE_PROFILES=demo
